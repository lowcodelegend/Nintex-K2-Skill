# Cross-artifact contracts

Apply these contracts before invoking any mutating specialist command.

## Artifact graph

Use the dependency direction `SQL objects → Service Instance/SmartObjects → views → forms → workflow → form integration rules`. Deploy and verify in that order. Cleanup, when authorized, runs in reverse order.

Names shared across manifests must resolve to the same K2 artifacts:

- SmartForm view bindings must name SmartObjects generated by the SQL Service Instance.
- Workflow SmartObject steps and references must use the same request SmartObject and identifier used by the entry form.
- Workflow `smartForms.form` must name a form declared in the SmartForms manifest.
- Specialist `application.rootCategoryPath` values must equal the solution root.

## Request workflow baseline

A typical request workflow must establish all of the following:

- A durable request identifier is created before workflow start and is available as the workflow's primary SmartObject reference.
- The request exposes a writable status property and the workflow explicitly maps both identifier and status inputs for every status update.
- Status values cover at least submission/pending, approved, and rejected outcomes using the application's vocabulary.
- Notifications use workflow identities and reference values. Fixed recipients require an explicit business reason.
- The user task has business actions, an enabled task notification when notification is required, and a participant resolved from a workflow identity or data reference.
- SmartForms Start integration starts the workflow after the form's create/save action succeeds.
- SmartForms Task integration opens the correct task state with its Serial Number context and exposes the configured task actions.

## Form-state invariant

For a dedicated request-entry form:

1. Base may exist as the design state.
2. Start must be the sole default runtime state.
3. Task must not be default.
4. The ordinary Runtime form URL must therefore use Start behavior without a state query parameter.
5. Clicking Create must save the item, populate the request reference, and start exactly one workflow instance.

For a shared form, never infer a default-state change. Choose and record one of: make Start default intentionally, preserve the default and use a state-specific entry URL, or create a dedicated request-entry form.

## Verification gates

1. Run every selected specialist's `doctor` and `plan` command.
2. Verify the database objects, Service Instance, generated SmartObjects, and representative SmartObject methods.
3. Verify form/view deployment, modern theme mode, required method input mappings, and browser CRUD behavior.
4. Verify workflow JSON shape, publication/version, SmartObject method mappings, dynamic recipients, task notification, and SmartForms integration.
5. Execute each end-to-end scenario from the ordinary Runtime entry URL.
6. Correlate the saved request identifier, workflow instance, worklist task, selected action, and final status. Do not accept independent smoke tests as equivalent evidence.

Record verification evidence and deployed identifiers in a deployment ledger. Current tools do not provide atomic cross-layer rollback; stop on failure and do not mutate dependants.

